spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/configdb
    username: config_user
    password: config_pass

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  redis:
    host: localhost
    port: 6379

  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    root: INFO
